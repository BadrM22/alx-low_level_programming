/**
 * _abs - Function to get the absolute value of an integer
 * @i: Integer number to get its absolute value
 *
 * Return: int
 */

int _abs(int i)
{
	if (i < 0)
		return (i * (-1));
	return (i);
}
